version: 0.2

phases:
  build:
    commands:
      - echo "=== Build phase start ==="
      - cp src/*.py .
      - echo "=== Build phase finished ==="

artifacts:
  files:
    - lambda_function.py
  discard-paths: yes
